
         ---- HTTPS ----
 (https://github.com/rituRaj-LUMIQ/ref_to_Push.git)




…or create a new repository on the command line

echo "# ref_to_Push" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/rituRaj-LUMIQ/ref_to_Push.git
git push -u origin main

…or push an existing repository from the command line

git remote add origin https://github.com/rituRaj-LUMIQ/ref_to_Push.git
git branch -M main
git push -u origin main







                ---- SSH ----
 (git@github.com:rituRaj-LUMIQ/ref_to_Push.git)



.....or create a new repository on the command line

echo "# ref_to_Push" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:rituRaj-LUMIQ/ref_to_Push.git
git push -u origin main



.....or push an existing repository from the command line

git remote add origin git@github.com:rituRaj-LUMIQ/ref_to_Push.git
git branch -M main
git push -u origin main